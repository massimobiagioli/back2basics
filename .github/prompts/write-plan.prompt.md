---
description: Create a structured implementation plan with phases, tasks, deliverables, and status tracking for a Back 2 Basics feature.
agent: write-plan
argument-hint: Describe the feature to plan (e.g. "language switcher", "playbook renderer")
---

## Example

```
/write-plan a language switcher component in the toolbar that lets users toggle between Italian and English. It must persist the choice in localStorage and update the URL path.
```
